export default function Divider() {
  return (
    <div className="mt-3 mb-0 h-px w-full" style={{
      background: 'linear-gradient(90deg, var(--accent-violet), var(--accent-cyan), transparent)',
      opacity: 0.5,
    }} />
  )
}
